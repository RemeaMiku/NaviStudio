<UserControl
    x:Class="MiraiNavi.WpfApp.Views.Pages.MapPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:gmaps="clr-namespace:GMap.NET.WindowsPresentation;assembly=GMap.NET.WindowsPresentation"
    xmlns:local="clr-namespace:MiraiNavi.WpfApp.Views.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    d:DataContext="{d:DesignInstance local:MapPage}"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid d:Background="White">
        <gmaps:GMapControl
            x:Name="GMap"
            CanDragMap="True"
            Loaded="OnGMapLoaded"
            MaxZoom="20"
            MinZoom="3"
            MouseWheelZoomType="MousePositionWithoutCenter"
            Zoom="3" />
        <ComboBox
            x:Name="MapTypeBox"
            HorizontalAlignment="Right"
            VerticalAlignment="Top"
            Background="{ui:ThemeResource ControlOnImageFillColorDefaultBrush}"
            SelectedIndex="0"
            SelectionChanged="ComboBox_SelectionChanged"
            Style="{StaticResource DefaultComboBoxStyle}">
            <ComboBoxItem Content="混合" Style="{StaticResource DefaultComboBoxItemStyle}" />
            <ComboBoxItem Content="街道" Style="{StaticResource DefaultComboBoxItemStyle}" />
            <ComboBoxItem Content="卫星" Style="{StaticResource DefaultComboBoxItemStyle}" />
        </ComboBox>
        <ui:Button
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Appearance="Primary"
            Command="{Binding ViewModel.ReplayCommand, Mode=OneTime}"
            Content="开始"
            Icon="Play24" />
    </Grid>
</UserControl>
